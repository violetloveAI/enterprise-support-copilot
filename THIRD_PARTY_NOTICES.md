# Third-party notices

Runtime dependencies retain their respective licenses. Direct dependencies are declared in
`pyproject.toml` and `frontend/package.json`. This repository contains no copied source files from
the acknowledged reference projects. The project-specific source code is released under `LICENSE`.

