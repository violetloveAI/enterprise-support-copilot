# Frontend V4

## V4.1 Worker 兼容性修复

- 将 GSAP 改为浏览器挂载后的动态加载，避免 Cloudflare Worker 在全局作用域启动计时器并触发 Error 1101。
- 保留原有入场、页面切换、证据链列表与详情面板动画，并补充卸载清理与回归断言。

## V4.0 视觉与交互优化

- 引入 GSAP 与 `@gsap/react`，为侧栏、顶栏、页面标题、卡片、表格、证据标签和诊断结果增加分层 timeline 动效。
- 所有进入动画使用 transform / autoAlpha，selector 限制在组件 scope 内，更新与卸载时自动清理。
- 使用 `gsap.matchMedia()` 区分桌面和移动位移，并尊重 `prefers-reduced-motion`。
- 建立 10–14px 的可读正文基线，重点修复运行记录、引用知识与右侧执行详情的小字号。
- 桌面执行详情栏从 382px 扩展至 420px，提升长文本可读性。
- 移动端运行记录改为卡片网格；调整证据标签、时间线、工具卡片、指标、代码区、弹窗和安全区。
- 增强卡片 hover、箭头反馈、进度条流动与服务状态呼吸效果。
