# Enterprise Support Copilot — FDE 面试手册

## 1. 一句话定位

这是一个面向企业 ERP 支持团队的“证据驱动故障诊断 Copilot”：它把知识检索、只读系统查询、证据校验、结构化诊断和高风险操作审批放在一条可观察、可审计、可演进的 Agent 工作流中。

## 2. 它解决什么问题

企业支持场景的困难不是“能不能生成一个听起来合理的答案”，而是：

- 制度知识、工单信息和系统状态分散在不同系统；
- 一线支持人员需要在有限时间内定位根因；
- 纯 LLM 容易在证据不足时补全事实；
- ERP 写操作涉及财务、权限和审计责任，不能让模型自行执行；
- 诊断过程必须能被支持经理、研发和审计人员复盘。

目标用户包括 L1/L2 支持、财务 IT、ERP 运维、解决方案工程师和事故负责人。产品将“回答问题”升级为“收集证据—验证结论—控制行动”的业务闭环。

## 3. 端到端工作流

1. `analyze_query`：识别意图、实体、问题分类和缺失上下文；
2. `retrieve_knowledge`：从企业知识库召回制度与 SOP；
3. `plan_tools`：根据问题选择最小集合的只读 ERP 工具；
4. `execute_tools`：通过受控 REST 边界查询系统事实；
5. `evidence_guard`：检查结论是否被本次工具结果和有效引用支持；
6. `compose_answer`：输出根因、风险、置信度、建议步骤和升级要求；
7. `create_ticket`：如需写操作，Graph 暂停并等待人工批准；
8. Audit：保存安全摘要、工具结果、引用和人工决策供复盘。

当前仓库同时支持两条路径：公开站点使用确定性 fixture，保证面试官无需密钥或后端也能体验；本地全栈会真实调用 FastAPI、LangGraph、知识检索和独立 Mock ERP REST API，并把 API 返回的检索、工具、事件和 HITL 状态映射到同一套 UI。

## 4. 这个项目能体现的 FDE 能力

| FDE 能力 | Demo 中的证据 | 面试时怎么说 |
| --- | --- | --- |
| 客户问题抽象 | 从“报销失败”抽象为知识、系统事实、策略和行动四类需求 | “我先定义可验证的业务闭环，而不是先选模型。” |
| 解决方案架构 | React Workbench、Agent API、RAG、Mock ERP 四层边界 | “每层都有清晰契约，可以先替换后端而不重写前端。” |
| AI 应用设计 | RAG、工具调用、证据门、结构化输出、HITL | “模型负责理解和组合，确定性代码负责权限与副作用。” |
| 企业集成 | REST 工具边界、只读优先、幂等键、审计字段 | “不直连 ERP 数据库，保留系统所有权和授权边界。” |
| 产品与 UX | 默认非空快照、渐进披露、移动端、reduced-motion | “Demo 的第一分钟必须让非技术 stakeholder 看见价值。” |
| 评测与可观测性 | 节点耗时、重试、引用、baseline 指标和运行记录 | “我区分离线评测、线上指标和业务成功指标。” |
| 安全与治理 | 不显示私有推理、证据不足时降级、写操作人工确认 | “LLM 可以提议，不能自行承担业务责任。” |
| 交付与调试 | 从 Error 1101 日志定位 GSAP 服务端副作用并修复 | “我能从部署症状追到运行时模型，并加回归防止复发。” |
| 沟通与取舍 | 明确 Demo、生产、指标和未来路线的边界 | “我宁愿诚实呈现确定性 fixture，也不伪装在线 AI。” |

## 5. 当前项目最欠缺的部分，以及如何诚实补齐

这不是对你个人经历的判断，而是项目本身相对完整 FDE 案例的差距。

### 缺口 A：后端已接通，但默认不使用外部模型

当前 deterministic + lexical 模式可以完整复现后端链路；配置 OpenAI-compatible endpoint 后也可进入模型 + vector 模式。但仓库没有保存真实模型密钥，也不能把 deterministic 结果当作模型效果。

面试回答：

> “我把稳定展示与工程验证拆开：公开站点保留离线 fallback，本地全栈真实经过 FastAPI、LangGraph、检索与 Mock ERP；模型和检索 provider 可以分别切换。这样既不会伪装在线 AI，也能复现真实接口和 HITL。”

建议补齐：选择一个模型供应商跑独立评测，重点记录结构化输出失败、错误高置信、工具选择和引用忠实度，不追求只展示最好分数。

### 缺口 B：评测指标仍是 baseline 展示

目前的 54 cases 与100%各项结果是 deterministic baseline，不是生产模型指标。

面试回答：

> “这些数字只描述仓库内 deterministic baseline。我不会把它们包装成模型准确率。真实版本会分别评估检索 recall@k、引用有效率、根因分类、工具选择、拒答和端到端任务成功率。”

建议补齐：在现有54条 ground truth 上增加歧义、冲突、工具超时、Prompt Injection 和应拒答样本，并单独报告模型模式。

### 缺口 C：缺少真实企业身份与数据治理

当前没有 SSO、RBAC、租户隔离、PII 脱敏和数据保留策略。

面试回答：

> “前端审批不是安全边界。生产版必须在工具网关和后端重新鉴权，并把用户、租户、作用域、目的和审批记录写入不可抵赖审计日志。”

建议补齐：画出 OIDC / RBAC / Tool Gateway / Audit Log 的威胁模型。

### 缺口 D：没有真实客户发现过程

FDE 不只是工程实现，还要证明需求发现和上线采用。

建议补齐：准备一个假设明确的 pilot 方案——访谈 5 位支持人员，选 2 类高频只读故障，运行 4 周，以 MTTR、升级率、证据覆盖率和人工采纳率作为成功指标。

## 6. 60 秒项目介绍

> “我做的是一个 Enterprise Support Copilot，目标是帮助 ERP 支持团队更快且更可信地定位报销、权限、凭证和接口问题。它先检索企业制度，再通过受控只读工具查询系统事实，并用 evidence guard 检查引用和工具证据是否来自本次运行。最终输出结构化根因、风险和处理步骤；需要创建工单时，LangGraph 会暂停并等待人工决定。项目有可离线演示的前端，也有已经接通的 FastAPI、LangGraph、检索、Mock ERP 和54条评测链路。所有数据都是合成的，基线结果不代表生产模型准确率。”

## 7. 3–5 分钟演示顺序

### 0:00–0:30 问题

说明企业支持的三个核心矛盾：知识分散、系统事实需要查询、写操作需要责任边界。

### 0:30–1:20 默认快照

先不运行，切换右侧三个标签，点开一个 Trace、一个 Tool 和一个 Knowledge：

- Trace 展示安全节点摘要，不展示私有推理；
- Tool 展示 REST request/response、延迟、重试和 `side_effect: false`；
- Knowledge 展示 chunk、检索分数和 citation validation；说明 lexical 与 vector 模式的边界。

### 1:20–2:20 运行一个场景

选择“公司范围 500”或“凭证生成失败”，点击开始。解释六个节点逐步解锁，突出“先只读取证，再形成结论”。

### 2:20–3:00 诊断与 HITL

展示根因、证据、建议步骤和引用。点击创建工单：先说明拒绝不产生写请求，再说明批准需要后端重新鉴权并以 `run_id` 保证幂等。

### 3:00–4:00 工程视图

解释 React UI、Agent API、RAG 和 ERP Tool Gateway 四层；指出 fixture 与真实 JSON API 使用同一状态模型。

### 4:00–5:00 评测与取舍

主动声明 baseline 边界，再说生产指标、失败策略和下一阶段计划。主动讲限制会增加可信度。

## 8. 面试问题与参考答案

### 产品与客户

#### Q1：这个产品为谁解决什么问题？

ERP 一线与二线支持经常需要在制度、单据状态、权限配置和接口日志之间切换。产品把这些证据收集到一次诊断运行里，目标是缩短 MTTR、减少无证据升级，并让高风险行动可控和可复盘。

#### Q2：为什么不用普通 Chatbot？

Chatbot 优化的是自然语言回答；这个场景需要状态、工具、证据、权限和审计。Agent workflow 只有在需要动态选择查询、组合多源证据时才有价值，确定性分类或规则仍应由普通代码完成。

#### Q3：为什么选择 ERP 支持？

它同时具备企业价值和技术约束：问题高频、知识密集、需要系统查询，而且写操作风险高，适合展示 FDE 如何把 AI 嵌入真实业务流程，而不是只做聊天界面。

#### Q4：你会怎么做客户发现？

先访谈支持人员和系统 owner，收集高频问题、当前排查步骤、数据权限、错误成本与升级路径；按频次、耗时、可取证性和风险评分，优先选择高频、只读、证据清晰的 1–2 类问题做 pilot。

#### Q5：Pilot 的成功指标是什么？

业务指标用 MTTR、一次解决率、升级率、建议采纳率和支持人员节省时间；质量指标用证据覆盖率、有效引用率、工具成功率和错误高置信率；安全指标用未授权写入必须为零。

### 架构与 Agent

#### Q6：为什么拆成六个节点？

因为理解、检索、工具规划、执行、证据校验和回答具有不同失败模式与可观测指标。拆分后可以独立重试、降级、评测和审计，也能在写操作前设置确定性中断。

#### Q7：为什么不让模型直接查询 ERP 数据库？

数据库直连会绕过 ERP 的鉴权、业务校验、审计和版本化契约。工具网关把能力限制为窄而可验证的 API，参数可校验、响应可脱敏、速率可控，也便于撤销权限。

#### Q8：什么时候用 RAG，什么时候用工具？

制度、SOP 和历史知识适合 RAG；实时单据状态、权限和接口健康度必须通过工具查询。最终结论需要区分“规范应该怎样”和“系统当前实际上怎样”。

#### Q9：Evidence guard 做什么？

它验证关键事实是否来自本次允许的工具结果或有效召回文档，引用是否存在、是否属于当前 run，并检查冲突和缺失。失败时降低置信度、请求补充信息或拒绝给出根因，而不是润色一个猜测。

#### Q10：为什么不展示 chain-of-thought？

私有推理既不是可靠解释，也可能暴露敏感上下文。界面展示可审计的安全摘要：节点、输入输出、工具证据、引用、策略和最终结构化结果。

#### Q11：真实后端如何接入？

`POST /api/v1/chat/invoke` 创建 run 并返回诊断、检索、工具和安全事件；Graph interrupt 后通过 `POST /api/v1/runs/{run_id}/resume` 提交 approve/reject。当前版本是同步 JSON，若真实模型延迟增长，再把事件层升级为 SSE，而不改变领域响应结构。

#### Q12：为什么区分 run_id 和 thread_id？

`thread_id` 表示跨轮会话或案件上下文，`run_id` 表示一次可审计执行。一个 thread 可以有多次 run；幂等、重试、指标和审批都应绑定具体 run。

### 安全与可靠性

#### Q13：Human approval 真的安全吗？

前端弹窗只是 UX，不是安全边界。后端必须在恢复 Graph 前重新鉴权、校验角色与作用域、记录审批者，并由工具网关再次执行策略检查。

#### Q14：如何避免重复创建工单？

使用 `run_id + action` 作为幂等键，写工具保存请求结果；超时重试先查询幂等记录，已成功则返回原工单号，而不是再次写入。

#### Q15：工具超时怎么办？

为每个工具设置短超时、有限重试和熔断。只读工具可按错误类型重试；写工具默认不盲目重试。失败事件进入 trace，证据不足时不生成确定根因，并给出人工下一步。

#### Q16：如何防 Prompt Injection？

把检索文档视为不可信数据，不把文档指令提升为系统指令；工具使用严格 schema 和 allowlist；敏感工具不由自由文本直接选择；输出经过策略与证据校验；记录可疑文档并支持隔离。

#### Q17：如何做多租户和权限？

身份通过 OIDC 进入后端，检索与工具调用都携带 tenant、user、role 和 scope；向量索引和缓存按租户隔离；工具网关执行 ABAC/RBAC；日志脱敏并遵循保留期限。

#### Q18：如果知识和系统结果冲突怎么办？

系统事实与制度知识承担不同角色。应显式标记冲突，不让模型自行决定真相；对过期制度降权或阻断，展示数据时间戳，并把冲突升级给知识 owner 或系统 owner。

### 评测与可观测性

#### Q19：当前100%代表什么？

只代表54条人工标注合成案例在 deterministic baseline-v2 上的分类、工具选择、Retrieval hit@3、升级判断、引用和证据引用校验结果。它不代表生产模型准确率，也不能证明真实故障解决率或业务价值。

#### Q20：你会如何设计评测集？

从真实脱敏工单抽样，覆盖正常、歧义、缺证据、工具失败、知识冲突和攻击样本；为每条定义期望分类、允许工具、关键证据、根因和是否应拒答。按版本固定数据集并保留失败分析。

#### Q21：最重要的线上监控是什么？

按节点监控 p50/p95 延迟、错误率、工具成功率、检索空结果、引用有效率、拒答率、审批率、重复写入和每 run 成本，并能从业务指标下钻到具体 trace。

#### Q22：置信度怎么得到？

不能直接把模型自报概率当真。应由可校准特征组合：检索分数、证据覆盖、工具一致性、冲突、历史验证结果和分类器校准；按风险设置阈值，并定期检查可靠性曲线。

### 工程与部署

#### Q23：这个 Demo 最有代表性的线上故障是什么？

V4 首次部署构建成功，但线上返回 Cloudflare Error 1101。Worker 日志显示 GSAP 在模块全局作用域启动计时器；本地 Node 允许这一行为，所以原测试没有捕获。修复是删除服务端静态导入，在 React 挂载后动态加载 GSAP，并用 `gsap.context()` 清理；同时加入“不允许静态 GSAP 导入”的回归测试。这个案例说明构建成功不等于运行时健康，部署后必须真实请求并检查平台日志。

#### Q24：为什么使用 GSAP？

不是为了装饰，而是让状态变化和信息层级更清楚：标题揭示、卡片 stagger、证据解锁和页面切换。动画只使用 transform/opacity，并尊重 reduced-motion；如果动画影响任务速度，就应删掉。

#### Q25：为什么现在选择中国境内静态部署？

HR 访问是当前真实约束。公开 Demo 保持静态可用，消除模型密钥、后端故障和跨境依赖；工程验证使用独立部署或本地运行的 API。两条路径在界面上必须清楚标识，不能把 fallback 说成 live run。

#### Q26：如何扩展到高并发？

静态 UI 走 CDN；run API 无状态水平扩展；长任务通过队列和可恢复 Graph；工具网关限流与熔断；检索服务和缓存按租户隔离；SSE 连接与任务执行解耦。先基于负载测试定位瓶颈，而不是预先过度设计。

#### Q27：最大技术债是什么？

当前 API client 已从页面组件拆出，但 UI、fixture 和状态管理仍集中在较大的 `SupportConsole.tsx`。下一步应拆分 domain types、scenario fixtures、state reducer 和 evidence components，并增加真实浏览器端到端测试。

### FDE 交付与沟通

#### Q28：客户要求“马上接真实 ERP”，你怎么处理？

先明确业务目标和风险，不直接开放数据库。用一个只读、低风险 endpoint 做 thin slice，定义数据 owner、权限、脱敏和验收指标；证明价值后再扩展工具面。

#### Q29：业务方和安全团队意见冲突怎么办？

把争论转成可验证的风险与控制：列出数据、动作、用户和失败后果；设计只读范围、审批、审计和 kill switch；用小流量 pilot 收集证据，再共同决定是否扩大。

#### Q30：如果模型效果不好，项目是不是失败？

不一定。先判断问题发生在检索、工具、编排还是任务定义。若高风险自动诊断不达标，可以把产品降级为“证据聚合与下一步建议”，仍可能显著缩短人工排查时间。

#### Q31：你在这个项目中具体做了什么？

必须按真实贡献回答。一个诚实模板是：

> “我负责问题定义、场景与信息架构、风险边界、验收标准和演示叙事，并使用 AI coding agent 加速实现。我逐项审阅交互、测试构建、分析线上日志并推动修复。哪些代码由工具生成、哪些决策由我做，我可以清楚说明。”

不要声称所有代码都是手写，也不要把“仓库中已经实现的全栈能力”描述成“已经接入生产 ERP 或真实客户上线”。

#### Q32：如果再给你一周，你会做什么？

优先用真实模型跑一组包含失败样本的独立评测，补 OIDC/RBAC 与审批身份威胁模型，并把 Agent API 部署到安全后端供公开 Demo 切换。不会先扩展更多 UI 场景。

## 9. 容易失分的说法

- “这是一个全栈生产 Agent。”——这是可复现的全栈作品，不是生产系统。
- “100% 是模型准确率。”——这是合成评测集上的 deterministic baseline。
- “有人工确认，所以写操作安全。”——前端确认不是安全边界。
- “RAG 能解决幻觉。”——只能提供证据，仍需校验、拒答和评测。
- “Agent 会自己选择任何工具。”——生产环境应使用 allowlist、策略和最小权限。
- “我展示了模型思维链，所以可解释。”——应展示证据与安全摘要。
- “我一个人手写了全部实现。”——按真实的人机协作方式说明贡献。

## 10. 面试前还应准备的材料

1. **离线备份**：本地 ZIP、`npm` 依赖缓存或已构建静态目录、2 分钟录屏、6 张关键截图。
2. **一页架构图**：UI、Agent API、RAG、Tool Gateway、ERP、OIDC/RBAC、Audit。
3. **一页评测设计**：数据集、指标、失败样本和上线门槛。
4. **一个真实 STAR 故事**：客户目标、约束、你的决定、冲突、量化结果。不要虚构。
5. **Demo 失败预案**：无网络时用 localhost 或录屏，仍能完成讲解。
6. **目标公司定制**：把 JD 中的 5 个关键词映射到本项目证据，准备为何该公司的具体答案。
7. **反问面试官**：团队如何选择客户问题、FDE 与产品/研究的边界、上线后的 ownership、成功指标和出差/驻场方式。

## 11. 建议反问面试官

- 你们如何判断一个客户需求应该做定制交付、产品化，还是拒绝？
- FDE 从 discovery 到 production 的 ownership 边界是什么？
- 团队最重视的成功指标是部署速度、采用率、收入还是可复用性？
- 目前最常见的失败来自模型能力、数据、集成，还是客户变更管理？
- 一个优秀 FDE 在入职 90 天后应能独立完成什么？

## 12. 下一轮定制所需输入

要把这份通用手册变成高命中版本，请补充：目标公司与 JD、面试轮次、面试语言、你的简历、你真实负责过的 2–3 个项目，以及最担心的技术或行为问题。
